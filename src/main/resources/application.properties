spring.application.name=MockInterview

# API Keys - Use environment variables
gemini.api.key=${GEMINI_API_KEY}
hume.api.key=${HUME_API_KEY}

# DB Configuration - Use environment variables
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/mockinterview}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=8av5mbxape@3byb
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# OAuth2 Configuration - Use environment variables
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri={http://localhost:8080/login/oauth2/code/google}/login/oauth2/code/google
